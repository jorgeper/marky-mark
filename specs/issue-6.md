# Spec: E2e coverage sweep for pane chevrons and slide animations (#6)

## Goal

All acceptance criteria in specs/issue-6.md are satisfied for issue #6, with evidence visible in the session: e2e coverage exists for every item of PRD 003 Requirement 13 — chevron close + edge-chevron reopen for both panes, tooltip and aria-label assertions for all four chevron controls (including the currently unasserted `preview-collapse`/`preview-expand` labels), menu/hotkey/Settings sync, the slide animation observed via polled `transform` interpolation, and W12's chevron-free web build — no test references the removed `folder-close` testid, and `npm run validate:quick` passes in the implementer's session.

## Acceptance criteria

This is a consolidation sweep over coverage largely landed by issues #3/#4/#5: audit each item below against the current suites, fill the gaps, and leave everything green. Where coverage already exists, verifying it (and citing the test ID) is sufficient — do not duplicate assertions for their own sake.

- E2e coverage exists for chevron close + edge-chevron reopen for both panes: the folder pane via `folder-collapse`/`folder-expand` (E94 in `tests/e2e/app.spec.ts` already covers this) and the split preview via `preview-collapse`/`preview-expand` (E84 already covers this, including placement at the pane edges). (Req 13)
- Tooltip (`title`) and `aria-label` assertions exist for all four chevron controls, extending E93's tooltip sweep. Folder chevrons are covered today (E93 asserts the `folder-collapse` title; E94 asserts `folder-collapse` aria-label and `folder-expand` title + aria-label). The preview chevrons' labels — "Hide the preview pane" / "Show the preview pane" per `src/components/FolderPanel.tsx:122` — are asserted nowhere and are the known gap: add `title` and `aria-label` assertions for `preview-collapse` and `preview-expand` (in E84 where both states already occur, or E93's sweep — implementer's choice). (Req 13)
- E2e coverage exists for menu/hotkey/settings sync: the folder chevrons vs View → Folders / `Mod+Shift+E` / reload persistence (E94, E95), and the preview chevrons vs View → Split Edit (including the native-menu checkbox), `Mod+\`, the Settings panel `set-split-edit` checkbox, and `settings.json` persistence (E84). (Req 13)
- E2e coverage exists for the slide animation observable via a polled `transform` interpolation, the way E25 does for the toolbar, for both panes — E133 already samples mid-flight `translateX` for folder and preview open/close, proves the pane outlives the close toggle, and covers `prefers-reduced-motion: reduce` switching instantly. (Req 13)
- W12 in `tests/e2e/web.spec.ts` confirms the web build stays chevron-free: `folder-collapse` and `folder-expand` have zero count in the web DOM in both pane states (already asserted at lines 376–377). If W12 or any web assertion changes, the web tier is verified with `npm run build:web` + `npm run test:e2e:web -- -g "W12"` (the web tier is outside `validate:quick`); if nothing in `web.spec.ts` changes, no web run is required. (Req 13)
- No test in the repo references the removed `folder-close` testid (a `grep -rn "folder-close" tests/` comes back empty — already true today; keep it that way). (Req 13)
- `npm run validate:quick` passes, run ONCE in the implementer's session right before declaring the goal met — not after every small change, and not as a start-of-attempt baseline. Iterate with `npm run typecheck` and `npm run test:unit`, or Playwright runs targeted at the touched tests (e.g. `npx playwright test -g "E84|E93|E94|E133"`), while developing.
- A summary comment from the implementer exists on issue #6.

## Context

Issues #3 (folder chevrons), #4 (preview chevrons), and #5 (slide animations) have all landed on this branch, and each brought its own tests, so most of Req 13 is already satisfied: E84 (`tests/e2e/app.spec.ts:2557`), E93 (:3063), E94 (:3147), E95 (:3216), E133 (:5666), and W12 (`tests/e2e/web.spec.ts:369`). The audit's one confirmed gap is the preview chevrons' tooltip/aria labels (`Hide the preview pane` / `Show the preview pane`, `src/components/FolderPanel.tsx:122` — the shared `PreviewToggleButton` renders both states). This issue is test-only; if the audit surfaces another Req-13 gap, fill it in the same style (assertions inside the existing E-numbered tests, matching their comment/idiom density) rather than adding new top-level tests unless one is genuinely needed. `validate:quick` runs version lock-step, typecheck, unit tests, and the desktop-shim e2e suite (`scripts/validate.mjs --quick`); the web e2e tier is separate (`playwright.web.config.ts` serves the built `dist-web`). PRD: `prd/003-pane-chevrons-and-slide-animations.md` (Requirement 13). Parent: #1.
