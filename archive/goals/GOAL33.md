# Launching the Marky Mark v33 build with /goal

Run from `~/src/marky-mark`. Prereq: review and approve
`docs/specs/SPEC33.md`. Infrastructure only — the product must be
untouched.

```
/goal Implement docs/specs/SPEC33.md in full (delta on SPEC.md–SPEC32.md as implemented; SPEC33 wins on conflict; ZERO product-behavior changes — no src/ runtime changes beyond none, no test-file changes, U1–U59/E1–E92/W1–W11 stay exactly as they are). Deliverable: the tiered developer workflow exactly as SPEC33 prescribes: (1) §1 scripts — scripts/validate.mjs gains --quick (version lock-step, typecheck, units, desktop e2e ONLY; prints 'QUICK VALIDATION: ALL PASSED', a distinct string; the full gate byte-identical in its step list); package.json gains validate:quick, build:app:fast (tauri build --debug --bundles app with the existing createUpdaterArtifacts:false override), install:app:fast (debug bundle path, same pkill/ditto/open flow), ship:local (quick gate → fast build → fast install). (2) §2 CI — release.yml drops its Windows job (tag ⇒ test gate → build-macos + build-web → draft with DMG/updater tar.gz/web HTML/SHA256SUMS over present assets, darwin-only latest.json); NEW release-windows.yml (workflow_dispatch, input tag): checkout tag → test gate → signed NSIS build → upload to the existing release → regenerate SHA256SUMS.txt over all assets → update the release latest.json with the windows entry → if the release is published, re-dispatch updater-manifest; RELEASING.md rewritten around the two-phase flow. (3) §3 docs/DEVELOPING.md — the tier table (task → command → what runs → rough time) for tauri dev / npm run dev, targeted vitest/playwright, validate:quick, full validate, ship:local, release-profile verify, mac cut, windows follow-up, plus the house feature rules; README links it from For developers. (4) §4 .claude/commands/ — five checked-in command files: check.md (validate:quick + diagnose failures, never weaken tests), gate.md (full validate + print the evidence block), dogfood.md (ship:local + confirm relaunch), release-mac.md (prepare→gate→licenses→push→tag→watch to draft→checksum-verify→STOP before publish unless the invocation says otherwise), release-windows.md (dispatch the workflow for a tag, watch, verify appended assets); each states steps, stop conditions, and the no-test-weakening rule. Done when: 'npm run validate' exits 0 with complete output — U1–U59, E1–E41 plus E45–E92, W1–W11, single-file check, bundle scan, 'VALIDATION: ALL PASSED' — printed in the transcript, AND 'npm run validate:quick' exits 0 printing 'QUICK VALIDATION: ALL PASSED' with no web-build/cargo/bundle/scan markers in its transcript, AND 'npm run build:app:fast' exits 0 printing the debug .app path, AND 'npm run ship:local' exits 0 with the app relaunched (pgrep evidence printed), AND actionlint or a printed YAML parse check passes for both workflow files with release.yml containing no windows job and release-windows.yml being workflow_dispatch with a tag input, AND docs/DEVELOPING.md exists with the README link, AND ls .claude/commands prints the five files, AND 'git diff src-tauri/' is empty, AND git diff shows no test-file or version-file or dependency changes, AND the reserved-name scan prints nothing. Constraints: the spec files and this condition must not be modified; tests must not be touched at all; the full validate step list must remain byte-identical (print the diff of scripts/validate.mjs and point to the guard in the transcript); do NOT trigger any real release or push any tag. Stop after 80 turns or 6 hours even if incomplete, and summarize remaining work.
```

## After it goes green (your part)

- Try the loop end to end: `npm run tauri dev`, poke a change, watch it
  hot-reload; then `/check` in a Claude session; then `/dogfood` and
  confirm the debug build relaunched.
- Next real feature: iterate in dev, `/check` mid-way, `/gate` at the
  end, `/dogfood` to live on it.
- Next release: `/release-mac 0.4.0-alpha.2` → smoke the draft →
  publish → later `/release-windows v0.4.0-alpha.2`.
