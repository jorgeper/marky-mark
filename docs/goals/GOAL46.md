# Launching the Marky Mark v46 build with /goal

Run from the feature worktree. Prereq: review and approve
`docs/specs/SPEC46.md` first — the goal implements exactly what it
prescribes.

```
/goal Implement docs/specs/SPEC46.md in full (delta on SPEC.md–SPEC45.md as implemented; SPEC46 wins on conflict, no regressions; this delta ships ZERO user-visible change — it is a work-reduction contract: byte-identical rendered HTML, canonical text, saved files, sidecars, drafts, and settings, with every SPEC1–SPEC45 debounce, ordering, and tolerance contract frozen per §0; out of scope per §7: incremental split-preview block diffing, splitting the preview injection effect, FindBar-local query state, and a batched list_dirs command — none may be smuggled in). The deliverable is five tiers of work reduction: Tier 1, the typing hot path (§2 — table detection confined to changed-line neighborhoods with spans untouched by a change never re-verified; one-slot identity-keyed caches for the doc string, the canonical text, and the buffer line-starts table shared across their consumers; canonicalizeAll as a single ascending pass; alignFilter taking its outside-every-grid early return before materializing the document and reusing one parse in the docChanged branch; Set-based parseDisplay separator membership; parseFrontMatter returning null on !startsWith('---') and splitting only through the closing fence; the editor value-sync effect skipping its own echo by emitted-string reference; diffDecorations and imageView refs cached on doc identity; computeChips reading sliceDoc(span) with its parse cached; hotkeys compiled once per map identity; the word-count chip early-returning when the setting is off, not re-firing on html in edit mode, and counting without materializing a token array); Tier 2, React rendering (§3 — the margin-card layout effect gains a cid→mark element index from the injection pass, a dependency array covering its React-flowing inputs, and a panel ResizeObserver so card positions stay pixel-identical; the selectionchange handler bails on unchanged {start,end,x,y}; dirty short-circuits on buffer===savedText; the derived comment lists memoize; FolderPanel is React.memo with memoized caps and stable handlers; applyThemeCss returns on identical text; applyActiveCues and the selection mirror run off the shared line-starts cache, a per-injection stamped-element list, and a per-injection pane getDocText cache, with rangeToOffsets walking to its boundary; ACTIVE_CONTAINERS hoisted, the auto-hide mousemove handler exiting under a native menu, onInsertImage stable); Tier 3, scroll sync (§4 — buildTable computed once per rebuild with table-taking mapper variants while lineAtOffset/offsetForLine remain as wrappers; the cue element cached in a ref written only by the cue lifecycle owners with its rect still read live each frame and the lookup moved after the end clamps; previewLeads skipping the CodeMirror dispatch within tolerance and stamping its quiet window only on an actual write; one pending rAF per scroll direction); Tier 4, startup and IPC (§5 — the five independent config loads, the per-theme reads, and the three listener registrations under Promise.all with SPEC30 explicit-opens-win untouched; every exists-preflight replaced by direct reads with rejection-as-missing including readDirEntries/readDirNames/mkdirp; the reopen timer at ≤50ms post-drain with concurrent liveness checks; a scroll pause at an unchanged top line writing nothing and the stamped-anchor sweep cached per rendered html and invalidated on zoom/font/margin changes; setAppMenu constructing one plain-option Menu.new tree preserving CheckMenuItem and accelerator-fallback handling, the fmOverride dep replaced by showFrontmatter, and byte-identical serialized specs not reinstalled; trash_entry as #[tauri::command(async)] with print_view staying sync); Tier 5, bundle and binary (§6 — main.tsx lazy-loading App and AuxWindow behind the windowRole switch with a trivial Suspense fallback; rehype-highlight dynamically imported on first render with the grammar set NOT subsetted; welcome.md as a direct raw import with the full FIXTURES glob moved into the shim's chunk and the THEMES guide lazy; panic="abort" and rlib-only crate-type in the release profile; the web platform skipping saveConfig when the written value is unchanged). Mechanism includes the §1 instrumentation seam: new pure src/lib/perfCount.ts, a no-op in production, enabled by the dev shim and exposed as window.__mmPerf with at minimum the counters §1 names (tableScan, parseDisplay, layoutTable, canonMiss/canonHit, syncTable, cueSweep, cardLayout, posWrite, menuInstall); counters are observability only — no app logic may read them. Done when: 'npm run validate' exits 0 with its complete output — U1–U80, E1–E41 plus E45–E133, W1–W11 — and the final line 'VALIDATION: ALL PASSED' printed in the transcript, AND E129–E133 and U77–U80 assert exactly the §8 contracts as work counts and byte-equivalences with no wall-clock assertion anywhere in the diff, AND 'git diff src-tauri/' touches only the Cargo.toml release-profile and crate-type lines and the trash_entry async attribute in lib.rs, AND the sanitize-schema diff is empty, AND the Windows-reserved-name scan (git ls-files | tr '/' '\n' | sort -u | awk -F. '{print tolower($1)}' | sort -u | grep -xE 'aux|con|prn|nul|com[0-9]|lpt[0-9]') prints nothing, AND ARCHITECTURE.md documents the perfCount seam and its measured-performance table is re-measured on this machine with cold launch, large-file open, theme switch, and mode toggle at or below their current printed values, AND 'git diff --stat docs/specs' is empty and 'grep -rEn "\.(skip|only|todo)\(" tests/' prints nothing. Constraints: the spec files and this condition must not be modified; existing tests may not be modified, weakened, stubbed, or deleted — the only permitted test additions are U77–U80 and E129–E133; no new dependencies (a dynamic import of the already-shipped rehype-highlight is not a new dependency); version files stay at 0.4.0-alpha.3; the SPEC11 network-isolation guarantee, sidecar/trailer formats, comment-anchor coordinate space, and all existing web behavior are unchanged; every cache introduced is one slot keyed on an immutable input's identity per §0.4 — a cache that could ever serve a stale value is a violation, not a trade-off. Stop after 80 turns or 8 hours even if incomplete, and summarize remaining work.
```

## After it goes green (your part)

Manual checks (the parts automation can't see):

- **§5.3 gate (required):** quit the app, double-click a `.md` file in
  Finder on a cold launch — the double-clicked file must open, not the
  reopened last document; repeat a few times (it's a race).
- Type fast in a large document containing a table (grid view on) —
  keystrokes should feel immediate; arrow through the table and watch
  the chips keep up while scrolling.
- Long momentum scroll in split mode on a big doc — both panes track
  without stutter; near the caret the cue alignment still locks on.
- Open Settings (⌘,) — the window should appear noticeably faster than
  before; flip toggles rapidly and watch the main window stay smooth.
- Add, resolve, and delete comments on a large doc — cards reposition
  exactly as before, no lag, no misplaced card after a reply edit.
- Trash a large folder from the sidebar — the UI must not freeze while
  the OS moves it to the Trash.
- Compare `du -h` of the built .app against the current release — it
  should be equal or smaller.
