# Spec: PRD 017 (5/5): documentation, enumerated test coverage and the validation gate (#191)

## Goal

All acceptance criteria in issue-specs/issue-191.md are satisfied for issue #191, with evidence visible in the session: the four documentation updates of PRD 017 Reqs 21–24 exist in docs/AUTHENTICATION.md, both hosting guides, server/README.md and prd/007-azure-hosted-workspaces.md; the enumerated unit coverage of Req 26 and the nine e2e scenarios of Req 27 exist in the test suites; `E2E_TEST_FLOOR` in scripts/validate.mjs is re-pinned to the new collected count citing PRD 017; and `npm run validate:quick` passes with `QUICK VALIDATION: ALL PASSED` in the implementer's session.

## Acceptance criteria

- **Req 21 — docs/AUTHENTICATION.md.** In § "Who can do what", the *"no lever"* paragraph (currently near line 173) is gone: the two open-to-everyone items are described as policy-controlled; the levers table has rows for `MM_ADMINS`, the creation policy and the listing policy; a short "Deployment admins" passage states the Req 4 implicit permission set and the visibility-by-membership rule in plain language; and the "Where the pieces live" table lists the admin routes, the settings module (`src/lib/deploymentSettings.ts`) and the Management component.
- **Req 22 — hosting guides.** `docs/HOSTING-AZURE.md` § 4 (environment variables) and `docs/HOSTING-AZURE-PORTAL.md` § 5.2 each document `MM_ADMINS` as optional, including how to find a user's object id (CLI: `az ad user show --id <upn> --query id -o tsv`; portal: Entra ID → Users → Object ID), plus a short "Deployment admins and policies" subsection pointing at the Management view for everything else. `tests/unit/docs-hosting-llm.test.ts` passes (`MM_ADMINS` is read by `server/config.ts`, so the parity check sees it).
- **Req 23 — server/README.md.** The README documents `MM_ADMINS` (partly present already), the `deployment/` prefix and settings record, the two deployment-level permission names, the implicit admin set, and the four admin routes in the API surface with their required permission, in the same style as the workspace routes.
- **Req 24 — prd/007.** `prd/007-azure-hosted-workspaces.md` Reqs 10 and 11 each carry one added sentence noting PRD 017 put the behaviour behind a deployment policy defaulting to the original behaviour. (This is the one permitted edit to a historical PRD; touch nothing else in it.)
- **Req 26 — unit coverage.** Unit tests exist (in `describe('PRD 017 §…')` blocks, numbered with the next unused `U` numbers — U993 is next free; tests already landed by #188–#190 count toward coverage, do not duplicate them) covering: `MM_ADMINS` parsing and the interior-whitespace refusal; the settings parser/serializer including Req 7's fail-closed defaults; the creation-policy and listing-filter decision functions across admin/member/guest/allow-listed callers; the Req 4 implicit-permission union; the Req 5 banner predicate; the Req 16 statistics aggregation over a synthetic `FileStat[]`; Graph `listUsers` paging and the caller-guest lookup with injected fetch; and the mock directory's `listUsers`. Likely homes: `tests/unit/deployment-admin.test.ts`, `deployment-settings.test.ts`, `server-deployment.test.ts`, `server-azure-providers.test.ts`, `server-mock-providers.test.ts`.
- **Req 27 — e2e coverage.** E2e tests exist in `tests/e2e/hosted.spec.ts` (next unused `E` numbers — E380 is next free; each test restores default deployment settings in a `finally` so the shared lane stays order-independent) covering the nine PRD scenarios: (1) a non-admin's `/api/me` says `admin: false`, no Management entry, and every admin route answers 403 `deployment.admin`; (2) the admin's Workspaces tab lists a non-member workspace with owners, member count, file count and size matching what was created; (3) the admin opens that workspace, reads a document, sees the banner, gets 403 `doc.edit` on save, self-adds as Owner in People, then saves; (4) Management delete behind the exact-name gate removes every blob under the workspace prefix; (5) under `restricted`, a regular user's start-page New Workspace is disabled with the restricted hint, the File-menu item is disabled, and `POST /api/workspaces` answers 403 `deployment.create`, while an allow-listed user creates normally; under `members`, `mary` is refused with the guest hint and `ada` is not; (6) under `members` listing, a non-member's `GET /api/workspaces` omits the workspace and the Open Workspace dialog does not show it, and the admin's ordinary listing is filtered the same way; (7) settings saved from the Settings tab are returned by `GET /api/admin/settings` after a reload, and a hand-corrupted settings blob produces fail-closed behaviour with the visible parse error; (8) the People tab lists all five seeded users with the Guest badge on `mary` and the Admin badge on `katherine`; (9) `/api/files` refuses the `deployment/` prefix. Scenarios already covered by existing E-numbered tests from #188–#190 count — extend or reference rather than duplicate.
- **Req 28 — floor and gate.** `E2E_TEST_FLOOR` in `scripts/validate.mjs` (currently 362 at line 286) is re-pinned to the new Playwright collected count with PRD 017 as the rationale in the adjacent comment, covering #190's E375–E379 too (Req 34 folds in here). `docs/MAP.md` has been regenerated via `npm run map` and committed if it changed.
- **Upgrade is a no-op.** Evidence in the session (code inspection or a targeted test) that a deployment running the PRD 016 build upgrades with no operator action and behaves identically until `MM_ADMINS` is set: azure mode with `MM_ADMINS` unset has zero admins, missing `deployment/settings.json` yields the defaults matching pre-PRD-017 behaviour (`everyone` creation, unfiltered listing), and no new required configuration exists.
- **Verification economy.** The implementer iterated with `npm run typecheck` and `npm run test:unit` (or tests targeted at the changed code, e.g. `npx playwright test -g '<title>'` for a single e2e), and ran the full gate `npm run validate:quick` ONCE, right before declaring the goal met — not after every small change and not as a baseline at the start.
- `npm run validate:quick` passes in the implementer's session, printing `QUICK VALIDATION: ALL PASSED`.
- A summary comment from the implementer exists on issue #191.

## Context

This is the close-out issue (5/5) of `prd/017-deployment-admins.md`; Reqs 21–28 and the Req 34 doc/test remainders are quoted nearly verbatim in the issue body. Issues #188–#190 already landed the feature code (`src/lib/deploymentAdmin.ts`, `src/lib/deploymentSettings.ts`, `server/admin.ts`, the Management view, invitations) plus some docs and tests — `server/README.md` already documents `MM_ADMINS` at line 70, and unit files like `tests/unit/deployment-admin.test.ts` exist. So audit before writing: each criterion is an end state; where #188–#190 already satisfy part of it, fill only the gaps. Highest used numbers today: E379, U992. The e2e floor check runs inside `validate:quick` (`scripts/validate.mjs` collects the desktop-shim suite), so a wrong re-pin fails the gate. `docs/MAP.md` is generated — never hand-edit it, run `npm run map`. The e2e suite is serialized machine-wide; use `-g '<title>'` for single tests during debugging. Citation comments (`// SPEC<n> §x.y` — see `.sandcastle/CODING_STANDARDS.md`) apply to any code you touch; find the owning spec numbers with `docs/MAP.md`.
