# Spec: Commit .claude/settings.json denying Read on archive/** and record the search-noise reduction (#36)

## Goal

All acceptance criteria in issue-specs/issue-36.md are satisfied for issue #36, with evidence visible in the session: a committed `.claude/settings.json` exists at the repository root whose `permissions.deny` denies `Read` on `archive/**` and denies nothing else (no Bash rules, nothing outside `archive/**`); before/after file counts for a `sidecar` search through the agent-facing search path are recorded in the implementer's summary comment on issue #36, with the reduction attributable to `archive/`; `npm run validate:quick` passes in the implementer's session; and a summary comment from the implementer exists on issue #36.

## Acceptance criteria

- `.claude/settings.json` exists at the repository root, is committed on this branch, and is valid JSON containing a `permissions.deny` entry that denies the `Read` tool on `archive/**` (use Claude Code's documented permission-rule syntax, e.g. `"deny": ["Read(./archive/**)"]`). This is PRD 005 requirement 25; the deny also suppresses `archive/**` from Grep and Glob results, which is the mechanism that removes the measured search noise.
- The deny list contains **no** rule constraining Bash (requirement 26), and no rule touching any path outside `archive/**` — nothing else in the repo loses tool access (requirement 29). The existing `.claude/commands/` and `.claude/skills/` directories are untouched.
- The search-noise reduction is recorded (requirement 47): the implementer's summary comment on issue #36 states the before and after file counts for a search for `sidecar` from the repository root, names the tool that produced each number, and attributes the reduction to `archive/**`. Baseline for reference: the PRD measured 65 files pre-archive-move; on this branch today, `grep -rl sidecar` (excluding `node_modules` and `.git`) finds 75 files, 28 of them under `archive/`. The "after" number is the count with `archive/**` suppressed — measured through the agent-facing search path (Grep/Glob) in a session that honours the deny rule, or, if the current session cannot demonstrate that (settings load at session start), computed as the same search excluding `archive/` with that method stated.
- An observation about requirement 28 is recorded in the implementer's summary comment: whether the committed `.claude/settings.json` is actually honoured by Sandcastle's containerised agent session (e.g. whether Grep/Glob in the implementer's own session suppress `archive/**` after the file exists). If it is not picked up automatically, the finding is recorded — the container-side fix is upstream (PRD non-goal) and does **not** block this issue.
- No file outside `.claude/settings.json` is modified by this change (requirement 27's README/AGENTS.md wording belongs to the AGENTS.md sub-issue, not this one).
- `npm run validate:quick` passes, run once in the implementer's session immediately before declaring the goal met. Iterate with `npm run typecheck` and `npm run test:unit` (or tests targeted at the changed code) during development — do not run the full quick gate after every small change, and do not run it as a starting baseline.
- A summary comment from the implementer exists on issue #36 describing what was done, carrying the before/after search numbers and the requirement-28 observation above.

## Context

The repo currently has no `.claude/settings.json` — only `.claude/commands/` and `.claude/skills/` — so this is a new file, not a merge. `archive/` already exists at the root (created by issue #33, merged) holding `goals/`, `superpowers/`, `articles/`, and a README. The PRD is `prd/005-agent-context-hygiene.md`, section D (requirements 25, 26, 28, 29) and evidence item 47; the parent issue is #23. Non-goal: this is a context guardrail, not a security boundary — Bash can still `cat` archived files, which is intentional. `package.json` scripts: `validate:quick`, `typecheck`, `test:unit` all exist; there is no plain `test` script.
