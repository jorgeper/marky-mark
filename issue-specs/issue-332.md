# Spec: PRD 025 sweep: per-build coverage, spec amendments and citations, rewritten and new e2e tests (#332)

## Goal

All acceptance criteria in issue-specs/issue-332.md are satisfied for issue #332, with evidence visible in the session: every build gets the PRD 025 (centered page layout) surfaces it has — a new W test proves the static web build renders the Edit/Preview toggle in `.edge-cluster` and not in the toolbar (Reqs 20, 27), and a new hosted-build E test proves the centered cluster with the page's radius and shadow under the hosted platform (Req 27); the six amended contracts (SPEC7 §5, PRD 003 Reqs 10–11, SPEC34, SPEC36 §4.1, PRD 013 Reqs 10–12, PRD 023 §14–15) each carry a PRD 025 amendment note and no live code comment still describes a slide, a lifted plane or the seam overlay as current (Req 29); the stale-name grep `rg -n 'preview-sliding|folder-slide|comments-slide|lift-row|lift-tab|split-edge-shade|workspace-stack::after' src tests docs` returns only historical notes because the sidebar and comments wrappers carry motion-neutral class names (Req 12 / landability note); E353, E306, the folder-tree plane assertions and the split-view / shell-and-menus / comments slide assertions are rewritten and the six new tests the PRD names exist (E578, E579, E581, E583, E584, E585) (Req 30); `docs/MAP.md` matches `npm run map`; `npm run validate:quick` has been run once at the end and prints `QUICK VALIDATION: ALL PASSED`; and a summary comment from the implementer exists on issue #332.

## Acceptance criteria

### Per-build coverage (PRD 025 Req 27, Req 20)

- **Static web build: the mode toggle's position is covered by a W test.** `tests/e2e/web.spec.ts` contains a new test `W20:` (next unused W id; bump if a parallel merge took it) that, on the built single-file page with the welcome document open, asserts: `page.getByTestId('edit-toggle')` has count 1; it sits inside `.edge-cluster` (the page-level control row at the page's top-right, `src/App.tsx` "PRD 025 Req 20" comment near the `edge-cluster` render); `page.locator('.toolbar').getByTestId('edit-toggle')` has count 0; there is no `file-tab-strip` (the static build has no `multiFileSession`). It cites `PRD 025 Reqs 20, 27 (issue #332)` in its title or body. If the toggle's render gate (`canToggleEdit`) is false on the static build's welcome document, the test asserts whichever of the `edge-cluster` members the gate does allow and the implementer's summary comment says so; do not change the gate.
- **Hosted build: the centered cluster is covered by an E test.** `tests/e2e/hosted.spec.ts` contains a new test (next unused E id — E587 at spec time; bump on collision) that boots a hosted workspace via the file's existing helpers, opens the folder sidebar and the comments column at a wide viewport (e.g. 1800 px, mirroring E583), and asserts: the folder wrapper's right edge equals `.workspace-stack`'s left edge and the page's right edge equals the comments wrapper's left edge (within 1 px); the ground left of the sidebar equals the ground right of the comments column (within 1 px, both > 0); `.workspace-stack` reports `borderTopLeftRadius` equal to the theme root's `--mm-radius-small` and a `boxShadow` other than `none`; the toggle `edit-toggle` is the last member of the strip's trailing control group. It cites `PRD 025 Req 27 (issue #332)`.
- **Desktop build coverage is unchanged and complete.** The existing desktop-shim tests stay as they are (never weakened, renumbered or skipped): E353 and E306 (`file-tabs.spec.ts`, flat strip / dark theme), E578 (`shell-and-menus.spec.ts`, no transition on pane toggle), E579 (`file-tabs.spec.ts`, toggle in the strip's group and in `.edge-cluster` with the strip off, gone from the toolbar), E581 (`folder-tree.spec.ts`, only the active row tinted), E583 (`file-tabs.spec.ts`, equal ground both sides, panes hug the page, one-pane cluster), E584 (`file-tabs.spec.ts`, Margins tracking editor-only and ×2 with preview), E585 (`file-tabs.spec.ts`, both panes closed full-width, no radius/shadow, no transition), W19 (`web.spec.ts`, static build both-panes-closed form and the 300 px comments column). Any of these missing or renamed is a regression the sweep restores.

### Spec amendments and citations (PRD 025 Req 29)

- **Each amended section carries a PRD 025 amendment note.** All six are present in the tree and stay present: `docs/specs/SPEC7.md` §5 (issue #328 note, Reqs 22–23), `prd/003-pane-chevrons-and-slide-animations.md` Reqs 9–12 retired (issue #328 note), `docs/specs/SPEC34.md` header row without rule (issue #329 note, Req 13), `docs/specs/SPEC36.md` §4.1 (issue #329 note for Reqs 14–15 and issue #331 note for Req 6), `prd/013-tabs-on-top.md` Reqs 10–12 retired (issue #331 note, Reqs 6, 17–18), `prd/023-comments-highlights-split.md` §14–15 (issue #331 note, Reqs 11–12). Verified with `rg -n 'PRD 025' docs/specs/SPEC7.md docs/specs/SPEC34.md docs/specs/SPEC36.md prd/003-*.md prd/013-tabs-on-top.md prd/023-comments-highlights-split.md` returning a hit in every file.
- **No live code comment describes the retired behaviour as current.** Comments citing the amended sections (`SPEC7 §5`, `PRD 003 Req 9–12`, `SPEC34`, `SPEC36 §4.1`, `PRD 013 Reqs 10–12`, `PRD 023 §14–15`) in `src/`, `editor/src/` and `tests/e2e/` describe the in-plane / flat / hugging behaviour or mark the old one as retired. Known stale site to fix: `editor/src/components/Preview.tsx` `initialHtml` doc comment ("so an opening slide animates over content from its very first frame") — reworded to the warm-start's surviving purpose (the pane paints content on its first frame; the slide is gone per PRD 025 Req 22). Verified by `rg -n -i '\bslides?\b|sliding' src editor/src tests/e2e` returning only lines that name the slide as retired/gone (issue #328, PRD 025 Req 12/22 notes) or that belong to unrelated features.

### Stale names (landability grep)

- **The sidebar and comments wrappers carry motion-neutral class names.** The `.folder-slide` wrapper (rendered in `src/components/FolderPanel.tsx`, `TocPanel.tsx`, `SearchPanel.tsx`; styled in `src/styles.css` including the `@media print` list) and the `.comments-slide` wrapper (rendered in `src/App.tsx`; styled in `src/styles.css` including the print list) are renamed to motion-neutral names (e.g. `.folder-wrap` / `.comments-wrap`), with every CSS rule, TSX `className`, code comment and e2e locator updated (`tests/e2e/shell-and-menus.spec.ts`, `file-tabs.spec.ts`, `web.spec.ts`, `comments.spec.ts`). No `data-testid` is renamed. Behaviour is unchanged: the folder wrapper still receives `--mm-folders` from the width drag, both wrappers are `display: none` in print, and E578/E583/E585/W19/E435 pass against the new names.
- **The grep returns only historical notes.** `rg -n 'preview-sliding|folder-slide|comments-slide|lift-row|lift-tab|split-edge-shade|workspace-stack::after' src tests docs` returns no live CSS selector, class attribute or locator — only comment lines that describe the treatment as gone (e.g. the `src/styles.css` token-block note, the SPEC36 / PRD 013 amendment notes) and the PRD text itself under `prd/`.

### Rewritten and new tests (PRD 025 Req 30)

- **Rewritten, not deleted.** The plane assertions in `file-tabs.spec.ts` (E353, E306) and `folder-tree.spec.ts`, and the slide assertions in `split-view.spec.ts`, `shell-and-menus.spec.ts` and `comments.spec.ts`, assert the new behaviour (flat strip / flat rows / instant mount) under their original IDs. No test in `tests/` is deleted, `.skip`ped or renumbered as part of this issue.
- **The six PRD-named new tests exist**, one per case: centered with equal ground at a wide viewport (E583), Margins tracking editor-only and ×2 with preview (E584), both-panes-closed full-width form (E585), absence of transitions on toggle (E578), mode toggle in the strip's group and absent from the toolbar (E579), only the active-file row tinted (E581). Each title carries a `PRD 025` citation.

### Map and gate

- **`docs/MAP.md` is current.** `npm run map` leaves `git status` clean for `docs/MAP.md` on the final commit (regenerate and commit it if any citation moved).
- **Iteration and the gate.** During the attempt the implementer iterates with `npm run typecheck`, `npm run test:unit`, and single targeted Playwright runs (`npx playwright test -g '<title>'`; the new W test via `npx playwright test --config playwright.web.config.ts -g 'W20'`). `npm run validate:quick` is run exactly once, right before declaring the goal met (not as a baseline, not after each change), and prints `QUICK VALIDATION: ALL PASSED`. Note that the quick tier does not run `web.spec.ts`; the new W test is proven by the targeted web-config run above, with its output visible in the session.
- **A summary comment from the implementer exists on issue #332** listing the new test IDs, the renamed classes, the citation fixes, and the gate result.

## Context

Siblings #328 (no pane motion), #329 (flat sidebar), #330 (mode toggle into the strip) and #331 (centered page) have all merged to `main`; this issue is the closing sweep of `prd/025-centered-page-layout.md` Reqs 27, 29, 30. The audit at spec time found the amendment notes, E353/E306 rewrites, the folder-tree/split-view/shell-and-menus/comments rewrites, the six new E tests and W19 already in place, and `npm run map` producing no diff. The real gaps are: no W test for Req 20 on the static build, no hosted-build test for the cluster form, the `.folder-slide` / `.comments-slide` wrapper names still live (only their transitions were removed), and the one stale slide comment in `editor/src/components/Preview.tsx`.

Pointers: the `edge-cluster` render and its `rightCluster` gate live in `src/App.tsx` around the `PRD 025 Req 20 (issue #330)` comment; `ModeToggleButton` is in `src/components/FolderPanel.tsx`. E583/E584/E585 in `tests/e2e/file-tabs.spec.ts` show the rect-measuring pattern to reuse for the hosted test; hosted tests boot through the helpers already used in `tests/e2e/hosted.spec.ts` (E358/E359 exercise the hosted strip). `playwright.web.config.ts` runs only `web.spec.ts` against `dist-web` (`npm run test:e2e:web`, needs `npm run build:web` first if `dist-web` is absent). Coding rules: `.sandcastle/CODING_STANDARDS.md` (citation comments, stable test IDs, `getByTestId` first, class locators only for DOM the app does not own or for theme structure). Beware that `prd/025-fluid-mode.md` also cites itself as "PRD 025": fluid-mode citations (issues #333–#337) are out of scope and must not be edited by the sweep.
