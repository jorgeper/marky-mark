# Spec: Release cut failed: W13 — web start page docname not visible/clickable on the built single-file page (#208)

## Goal

All acceptance criteria in issue-specs/issue-208.md are satisfied for issue #208, with evidence visible in the session: W13 passes against a freshly built single-file web page (`npm run build:web && npx playwright test --config playwright.web.config.ts -g 'W13'`), the whole web e2e suite (`npm run test:e2e:web`) passes once against that build, `npm run validate:quick` passes in the implementer's session, and a summary comment from the implementer exists on issue #208 stating whether the empty-docname invisibility was a user-visible bug or a stale test target.

## Acceptance criteria

- W13 ("the single-file web start page offers the drag hint and Open File — and nothing it cannot honour") passes deterministically against the built page: `npm run build:web && npx playwright test --config playwright.web.config.ts -g 'W13'` has been run in the implementer's session and exits green.
- The root cause is determined and stated in the implementer's summary comment on issue #208: either the empty `docname` span's invisibility on the web start page is a user-visible toolbar defect on the built single-file page (then the app/CSS is fixed), or it is only a stale click target in the test (then W13 closes the menu another sanctioned way). The fix matches the determination — do not change both the app and the test's close mechanism without saying why.
- W13's intent is preserved: the app menu is verifiably closed before the Open File / filechooser step, and the test's frozen assertions (drag hint, capability-driven start actions, hamburger-left-of-docname layout, menu row order, disabled Save / Save As…) are unchanged. Only the menu-closing step at `tests/e2e/web.spec.ts:474` may change if the test is the stale part.
- If `src/` code or CSS changed, each changed behaviour site carries the citation comment required by `.sandcastle/CODING_STANDARDS.md` (`// SPEC<n> §x.y` / PRD reference), consistent with the contracts already cited around the touched lines.
- The full web e2e suite has been run ONCE against a fresh `npm run build:web` via `npm run test:e2e:web` and passes — required because `validate:quick` does not cover the web suite (that gap is how this regressed), and the fix touches shared toolbar chrome that the other W-tests exercise.
- `npm run validate:quick` has been run ONCE in the implementer's session, right before declaring the goal met, and prints `QUICK VALIDATION: ALL PASSED`.
- Iteration discipline: the implementer iterated with `npm run typecheck` and `npm run test:unit` (plus the single targeted `-g 'W13'` run above when touching the behaviour), and ran the two full commands above only once at the end — not after every small change, and not as a baseline at the start.
- A summary comment from the implementer exists on issue #208 describing the root cause, the chosen fix, and the verification evidence.

## Context

The v0.5.0-alpha.3 cut (#207) failed the full gate: W13 times out at `tests/e2e/web.spec.ts:474` clicking `getByTestId('docname')` to close the app menu — the span resolves but is "not visible". On the web start page no document is open, so `.docname` (`src/components/Toolbar.tsx:228`) renders empty; a zero-size element is unclickable to Playwright. Two changes landed since the last full gate (v0.5.0-alpha.2, 2026-08-04): issue #197 removed the badge that used to occupy the empty title slot (comment at `Toolbar.tsx:240`), and the PRD 018 chrome migration (#202–#204, commits bbb87ce/f339976/e89d2ef) rewrote `.toolbar` / `.toolbar .docname` CSS (`src/styles.css:535`, `font-size: 13px` → `var(--mm-text-body)`, tokens declared on `.theme-root`). Decide first whether the collapsed empty docname is itself the user-visible bug (toolbar layout on the built page) before touching the test; if only the test is stale, `Escape` already fires at line 473 and asserting the menu is gone (`app-menu` count 0) or clicking a genuinely visible element are sanctioned closes. Note `src/App.tsx:7334` mentions `.docname` swallowing clicks — grep before changing its layout. Web e2e runs only in the full gate, never in `validate:quick`, hence the explicit `test:e2e:web` criterion.
